<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alpha">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestScoresActivity"></activity>
        <activity android:name=".WorkExperienceActivity" />
        <activity android:name=".LettersOfRecommendationActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".EducationalDetailsActivity" />
        <activity android:name=".BasicInfoActivity" />
        <activity android:name=".AspirationsActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Step2Activity" />
        <activity android:name=".Step1Activity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>